# Automation tool

## Overview
This project uses Python and OCR (pytesseract) with a GUI (tkinter) to reduce repetitive in-game tasks through user-defined screen selections.

## Tools Used

- Python / pytesseract / tkinter
